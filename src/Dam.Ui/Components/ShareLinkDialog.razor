@using MudBlazor
@using Dam.Ui.Services

@inject IUserFeedbackService Feedback
@inject IJSRuntime JS

<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h6">
            <MudIcon Icon="@Icons.Material.Filled.Share" Class="mr-2" Color="Color.Success" />
            Share Link Created
        </MudText>
    </TitleContent>
    <DialogContent>
        <MudStack Spacing="3">
            <MudAlert Severity="Severity.Success" Dense="true">
                Your share link has been created successfully!
            </MudAlert>

            <MudTextField Value="@ShareUrl"
                          Label="Share URL"
                          ReadOnly="true"
                          Variant="Variant.Outlined"
                          Adornment="Adornment.End"
                          AdornmentIcon="@Icons.Material.Filled.ContentCopy"
                          OnAdornmentClick="CopyUrlToClipboard" />

            <MudTextField Value="@Password"
                          Label="Password"
                          ReadOnly="true"
                          Variant="Variant.Outlined"
                          InputType="@(_showPassword ? InputType.Text : InputType.Password)"
                          Adornment="Adornment.End"
                          AdornmentIcon="@(_showPassword ? Icons.Material.Filled.VisibilityOff : Icons.Material.Filled.Visibility)"
                          OnAdornmentClick="TogglePasswordVisibility" />

            <MudStack Row="true" Spacing="2">
                <MudButton Size="Size.Small"
                           Variant="Variant.Text"
                           StartIcon="@Icons.Material.Filled.ContentCopy"
                           OnClick="CopyPasswordToClipboard">
                    Copy Password
                </MudButton>
                <MudButton Size="Size.Small"
                           Variant="Variant.Text"
                           StartIcon="@Icons.Material.Filled.ContentCopy"
                           OnClick="CopyAllToClipboard">
                    Copy All
                </MudButton>
            </MudStack>

            @if (ExpiresAt.HasValue)
            {
                <MudText Typo="Typo.body2" Color="Color.Secondary">
                    <MudIcon Icon="@Icons.Material.Filled.Schedule" Size="Size.Small" Class="mr-1" />
                    Expires: @ExpiresAt.Value.ToLocalTime().ToString("f")
                </MudText>
            }

            <MudAlert Severity="Severity.Warning" Dense="true">
                <MudIcon Icon="@Icons.Material.Filled.Lock" Size="Size.Small" Class="mr-1" />
                This link is password protected. Share the password securely with recipients.
            </MudAlert>
        </MudStack>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Close">Close</MudButton>
        <MudButton Color="Color.Primary" 
                   Variant="Variant.Filled"
                   StartIcon="@Icons.Material.Filled.ContentCopy"
                   OnClick="CopyAllToClipboard">
            Copy All
        </MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter]
    private IMudDialogInstance MudDialog { get; set; } = default!;

    [Parameter]
    public string ShareUrl { get; set; } = "";

    [Parameter]
    public string Password { get; set; } = "";

    [Parameter]
    public DateTime? ExpiresAt { get; set; }

    private bool _showPassword = false;

    private void Close() => MudDialog.Close();

    private void TogglePasswordVisibility() => _showPassword = !_showPassword;

    private async Task CopyUrlToClipboard()
    {
        try
        {
            await JS.InvokeVoidAsync("navigator.clipboard.writeText", ShareUrl);
            Feedback.ShowSuccess("Link copied to clipboard!");
        }
        catch
        {
            Feedback.ShowWarning("Failed to copy to clipboard. Please copy manually.");
        }
    }

    private async Task CopyPasswordToClipboard()
    {
        try
        {
            await JS.InvokeVoidAsync("navigator.clipboard.writeText", Password);
            Feedback.ShowSuccess("Password copied to clipboard!");
        }
        catch
        {
            Feedback.ShowWarning("Failed to copy to clipboard. Please copy manually.");
        }
    }

    private async Task CopyAllToClipboard()
    {
        try
        {
            var text = $"Share Link: {ShareUrl}\nPassword: {Password}";
            await JS.InvokeVoidAsync("navigator.clipboard.writeText", text);
            Feedback.ShowSuccess("Link and password copied to clipboard!");
        }
        catch
        {
            Feedback.ShowWarning("Failed to copy to clipboard. Please copy manually.");
        }
    }
}
