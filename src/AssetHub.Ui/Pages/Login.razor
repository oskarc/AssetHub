@page "/login"
@using Microsoft.AspNetCore.Authorization

@attribute [AllowAnonymous]

@inject NavigationManager Nav
@inject IStringLocalizer<CommonResource> CommonLoc

<PageTitle>@CommonLoc["Btn_SignIn"] - @CommonLoc["AppName"]</PageTitle>

<MudContainer MaxWidth="MaxWidth.Small" Class="d-flex align-center justify-center login-container">
    <MudPaper Class="pa-8 login-card" Elevation="4">
        <MudStack Spacing="4" AlignItems="AlignItems.Center">
            <MudIcon Icon="@Icons.Material.Filled.Collections" 
                     Color="Color.Primary" 
                     Size="Size.Large" 
                     Class="login-icon" />
            
            <MudText Typo="Typo.h4" Align="Align.Center">
                @CommonLoc["AppName"]
            </MudText>
            
            <MudText Typo="Typo.body1" Color="Color.Secondary" Align="Align.Center">
                @CommonLoc["AppSubtitle"]
            </MudText>

            <MudDivider Class="my-4" FlexItem="true" />

            <MudText Typo="Typo.body2" Align="Align.Center" Class="mb-4">
                @CommonLoc["Text_SignInDescription"]
            </MudText>

            <MudButton Variant="Variant.Filled"
                       Color="Color.Primary"
                       Size="Size.Large"
                       FullWidth="true"
                       StartIcon="@Icons.Material.Filled.Login"
                       OnClick="SignIn">
                @CommonLoc["Btn_SignIn"]
            </MudButton>

            <MudText Typo="Typo.caption" Color="Color.Secondary" Align="Align.Center" Class="mt-4">
                @CommonLoc["Text_RedirectIdentityProvider"]
            </MudText>

            <MudText Typo="Typo.caption" Color="Color.Secondary" Align="Align.Center" Class="mt-2">
                @CommonLoc["Text_ForgotPasswordHint"]
            </MudText>
        </MudStack>
    </MudPaper>
</MudContainer>

@code {
    [SupplyParameterFromQuery]
    public string? ReturnUrl { get; set; }

    private void SignIn()
    {
        var returnUrl = string.IsNullOrWhiteSpace(ReturnUrl) ? "/" : ReturnUrl;
        Nav.NavigateTo($"/auth/login?returnUrl={Uri.EscapeDataString(returnUrl)}", forceLoad: true);
    }
}
